import { ConfidencePipe } from './confidence.pipe';

describe('ConfidencePipe', () => {
  it('create an instance', () => {
    const pipe = new ConfidencePipe();
    expect(pipe).toBeTruthy();
  });
});
