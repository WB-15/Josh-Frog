import { ShipmentStatusPipe } from './shipment-status.pipe';

describe('ShipmentStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ShipmentStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
