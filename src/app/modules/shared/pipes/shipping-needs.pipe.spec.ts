import { ShippingNeedsPipe } from './shipping-needs.pipe';

describe('ShippingNeedsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShippingNeedsPipe();
    expect(pipe).toBeTruthy();
  });
});
