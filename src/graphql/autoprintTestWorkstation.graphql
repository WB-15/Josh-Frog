mutation autoprintTestWorkstation ($machineKey: String!) {
    autoprintTestWorkstation(
        machineKey: $machineKey
    ) {
        name,
        machineKey,
        warehouse {
            name
        }
    }
}
