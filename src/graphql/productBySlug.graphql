query productBySlug($slug: String!) {
  productBySlug(slug: $slug) {
    data {
      id
      slug
      name
      routePath
      parent {
        id
      }
      children {
        id
      }
    }
  }
}
