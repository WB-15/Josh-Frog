mutation autoprintGetNextForPrinter ($machineKey: String!, $printerName: String!) {
    autoprintGetNextForPrinter(
        machineKey: $machineKey,
        printerName: $printerName
    ) {
        name,
        printer,
        tray,
        rotate,
        dataBase64
    }
}
